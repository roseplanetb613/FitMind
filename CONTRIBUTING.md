# 参与贡献

欢迎。这里没有官样文章，只有几条真实起作用的约定。

## 怎么提

1. Fork → 改 → PR。PR 描述里说清**为什么**，比罗列改了什么更重要——
   本仓维护者的第一个问题永远是"为什么这么改"。
2. 一个 PR 一件事。想做大重构，先开 Issue 把取舍对齐再动手。
3. commit message 用中文、说人话：第一行讲清"改了什么"，正文讲清"为什么"。

## 改动必须带的

- **测试**。后端 `app/tests`（pytest）、前端 `web/tests`（vitest，node 环境、无
  jsdom）。改行为必须同步改测试，新功能必须带测试，全绿再提 PR。
- **类型检查**。前端 `tsc --noEmit` 必须过——vitest 不做类型检查，
  类型错误会"测试全绿"地溜过去。
- **理由写在代码里**。本仓每个非常规写法都带"为什么"注释，因为你加的代码
  三个月后要能被陌生人（包括未来的你）看懂。

## 三条红线（碰到必拒）

1. **素材授权**。`data/exercises-dataset/` 的动作图示 © Gym visual（经书面许可
   非商用分发、每次使用带署名，见 `data/exercises-dataset/NOTICE.md`）；
   `web/public/models/` 的 3D 模型是 Z-Anatomy 的 CC BY-SA 4.0。
   这两套授权的边界不能动，署名不能删——删署名等于让整个仓库下架。
2. **健康边界**。系统做风险分级与提示，**不做诊断、不给医学结论**；
   运动医学数据全部标记【待审】。这不是技术限制，是产品定位。
3. **数据诚实**。缺值返回 `None` 并说明原因，**不许拿 0 或默认值冒充**；
   外部依赖不可用时降级，绝不抛出中断主链路。

## 本地跑起来

见 [README](README.md) 的「快速开始」。跑测试不需要 Neo4j 在线
（离线时部分用例转 skip，比对前先数 skip）。
